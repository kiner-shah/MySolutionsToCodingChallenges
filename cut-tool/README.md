Implemented cut tool as per the [challenge description](https://codingchallenges.fyi/challenges/challenge-cut).

Doesn't work for following: `cut -d, -f '1,2 3' filename`. Multiple delimiters in field is not supported for now. `cut` tool of coreutils supports this.