Currently implemented a recursive-descent parser. But there are some problems with the recursive approach and it's not really efficient.

Future improvements:
- Implement a bottom-up parser like LALR or Shift-reduce.
